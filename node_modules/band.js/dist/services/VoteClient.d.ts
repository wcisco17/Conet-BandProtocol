import Web3 from 'web3';
import BN from 'bn.js';
import BaseClient from './BaseClient';
import { Address } from '../typing';
export default class VoteClient extends BaseClient {
    private sendAddress;
    constructor(sendAddress: Address, web3?: Web3);
    createCastVoteTransaction(onChainId: number, yesVote: string | BN, noVote: string | BN): Promise<import("./Transaction").default>;
    createCommitVoteTransaction(onChainId: number, commitHash: string, totalWeight: string | BN): Promise<import("./Transaction").default>;
    createRevealVoteTransaction(onChainId: number, yesVote: string | BN, noVote: string | BN, salt: string | BN): Promise<import("./Transaction").default>;
    getVotingPower(pollId: number): Promise<BN>;
    private getRequestVote;
    private postRequestVote;
}
