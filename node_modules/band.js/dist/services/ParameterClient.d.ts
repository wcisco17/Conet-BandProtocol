import BaseClient from './BaseClient';
import Web3 from 'web3';
import BN from 'bn.js';
import { Address } from '../typing';
export default class ParameterClient extends BaseClient {
    private coreAddress;
    constructor(coreAddress: Address, web3?: Web3);
    createProposalTransaction(reasonHash: string, keys: string[], values: (string | BN)[]): Promise<import("./Transaction").default>;
    createCastVoteTransaction(proposalId: number, isAccepted: boolean): Promise<import("./Transaction").default>;
    private postRequestParameter;
}
