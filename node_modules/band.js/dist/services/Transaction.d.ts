import Web3 from 'web3';
import { Address } from '../typing/index';
export default class Transaction {
    private web3;
    private sender;
    private to;
    private data;
    constructor(web3: Web3, sender: Address, to: Address, data: string);
    getTxDetail(): {
        sender: string;
        to: string;
        data: string;
    };
    send(): import("web3-core").PromiEvent<import("web3-core").TransactionReceipt>;
    sendFeeless(): Promise<{}>;
}
