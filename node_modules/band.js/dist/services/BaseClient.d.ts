import Web3 from 'web3';
import { Address } from '../typing';
import Transaction from './Transaction';
/**
 * This is a BaseClient Class
 */
export default class BaseClient {
    protected web3?: Web3;
    protected constructor(web3?: Web3);
    protected getAccount(): Promise<Address>;
    protected isLogin(): boolean;
    protected createTransaction(to: Address, data: string): Promise<Transaction>;
}
